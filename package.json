{
  "name": "node_demo_project",
  "version": "1.0.0",
  "description": "The sample Node.js project with React frontend",
  "scripts": {
    "start": "bun run server.js",
    "dev": "concurrently \"bun run server.js\" \"cd client && bun run dev\"",
    "build": "cd client && bun run build",
    "install:all": "bun install && cd client && bun install"
  },
  "dependencies": {
    "express": "^4.17.3",
    "oracledb": "file:/Users/daniel/node-oracledb",
    "concurrently": "^8.2.2",
    "pg": "^8.11.3"
  }
}